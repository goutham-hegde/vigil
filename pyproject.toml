[project]
name = "vigil"
version = "1.0.0"
description = "AI-driven threat detection and attack simulation engine with a SOC dashboard"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 120
